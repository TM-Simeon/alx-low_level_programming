#include "main.h"
/**
 * _strcmp - called function
 * @s1: first parameter
 * @s2: second parameter
 *
 * Return: 0 always
 */
int _strcmp(char *s1, char *s2)
{
	int a;

	a = strcmp(s1, s2);
	return (a);
}

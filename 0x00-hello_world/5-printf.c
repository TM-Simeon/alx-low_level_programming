#include<stdio.h>
/**
 * main - A program that prints \" with proper grammar, but ..
 *
 * Return: return zero if the program works correctly
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}

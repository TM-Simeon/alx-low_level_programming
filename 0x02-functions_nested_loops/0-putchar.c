#include <stdio.h>
/**
 * main -print _putchar
 *
 * Return: 0 for success
 */
int main(void)
{
	char p[8] = {'_', 'p', 'u', 't', 'c', 'h', 'a', 'r'};
	int i;
	for (i = 0; 1 < 8; i++)
	{
		putchar(p[i]);
	}
	putchar('\n');
	return (0);
}

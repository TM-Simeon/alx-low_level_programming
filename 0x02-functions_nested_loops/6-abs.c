#include "main.h"
/**
 * _abs - find the absolute value
 * @n : the integer to use
 *
 * Return: 0 always
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n == 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
